import { CryptoProvider, FxError, Result } from "@microsoft/teamsfx-api";
export declare class LocalCrypto implements CryptoProvider {
    private cryptr;
    private prefix;
    constructor(projectId: string);
    encrypt(plaintext: string): Result<string, FxError>;
    decrypt(ciphertext: string): Result<string, FxError>;
}
//# sourceMappingURL=crypto.d.ts.map